stages:
  - build
  - deploy 

before_script:
  - apt-get update -qq && apt-get install -y -qq python3 python3-pip jq
  - pip3 install awscli virtualenv
  
build:
  stage: build
  script:
    - ./bin/build.sh

deploy:
  stage: deploy
  script:
    - ./bin/deploy.sh

  